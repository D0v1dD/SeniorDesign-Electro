<resources>
    <style name="Theme.MyApplication" parent="Theme.MyApplication">
        <!-- Overriding specific attributes for API 23+ -->
        <!-- For example, edge-to-edge display -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
